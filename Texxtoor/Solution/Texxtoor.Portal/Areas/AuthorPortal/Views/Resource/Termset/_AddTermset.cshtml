@model Texxtoor.DataModels.Models.Author.TermSet
@using (Html.BeginForm("AddTermset", "Resource", FormMethod.Post, new{id = "addTermsetForm"})){
  using (Html.BtFieldset(Loc("legend", @"New Termset"))){
    @Html.EditorForModel()
    @Button.FormSendButtons("addTermsetSendButton", ViewResources.Action_Termset_CreateTermset, "addTermsetCancelButton", ViewResources.Button_CancelAndClose)
  }
}
