@model Texxtoor.DataModels.Models.Author.Team
@using (Html.BeginForm("AddTeam", "Team", FormMethod.Post, new{id = "addTeamForm"})){
  using (Html.BtFieldset(Loc("legend", @"New Team"))){
    @Html.EditorForModel()
    @Button.FormSendButtons("addTeamSendButton", ViewResources.Action_Team_CreateTeam, "addTeamCancelButton", ViewResources.Button_CancelAndClose)
  }
}