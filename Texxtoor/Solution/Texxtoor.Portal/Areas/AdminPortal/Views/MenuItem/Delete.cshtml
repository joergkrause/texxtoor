@model Texxtoor.DataModels.Models.Cms.CmsMenuItem

@{
    PageData["Title"] = "Delete";
    Layout = Url.GetGlobalLayout("_LayoutAdmin", "AdminPortal");
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
@using(Html.BtFieldset("CmsMenuItem")) {

    <div class="display-label">Title</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Title)
    </div>

    <div class="display-label">NavigateUrl</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.NavigateUrl)
    </div>

    <div class="display-label">IsExternalUrl</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.IsExternalUrl)
    </div>

    <div class="display-label">Visible</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Visible)
    </div>

    <div class="display-label">LocaleId</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.LocaleId)
    </div>

    <div class="display-label">IsFallback</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.IsFallback)
    </div>
}
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
