@model Texxtoor.DataModels.Models.Cms.CmsMenu

@{
    PageData["Title"] = "Delete";
    Layout = Url.GetGlobalLayout("_LayoutAdmin", "AdminPortal");
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
@using(Html.BtFieldset("CmsMenu")) {

    <div class="display-label">Title</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Title)
    </div>

    <div class="display-label">Type</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Type)
    </div>

    <div class="display-label">Description</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Description)
    </div>

    <div class="display-label">LocaleId</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.LocaleId)
    </div>

    <div class="display-label">IsFallback</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.IsFallback)
    </div>
}
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
